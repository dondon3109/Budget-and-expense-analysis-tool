# Copy this file to keystore.properties next to it and fill in the real
# values for local release builds. keystore.properties is gitignored;
# never commit it and never commit these passwords.
storeFile=/absolute/path/to/zoption-release.keystore
storePassword=CHANGE_ME
keyAlias=zoption-release
keyPassword=CHANGE_ME
